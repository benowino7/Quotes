import { QuoteLikePipe } from './quote-like.pipe';

describe('QuoteLikePipe', () => {
  it('create an instance', () => {
    const pipe = new QuoteLikePipe();
    expect(pipe).toBeTruthy();
  });
});
